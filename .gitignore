# 本地浏览器 profile：约 14MB，且 Local Storage 里存着「接口配置」（含 API Key 明文）
.edge-profile/

# E2E 运行产物：日志、崩溃转储、结果文件、页面截图，每次跑都会重新生成
.e2e/
_e2e-result.json
test-browser-arena.png

# 本地接口配置：可能含 API Key，不要提交（详见 README 的「配置与安全」）
.arena-config.json
arena-config.json
arena-config.local.json

# 打包副本
*.7z
*.zip

# 本机 AI 工具的项目级配置与记忆（工作区作用域）。这些是个人笔记，不属于项目内容，
# 正常它们存在用户目录下、不进仓库；这条规则是防止将来作用域改到工作区时被误提交。
.zcode/

# 操作系统
Thumbs.db
Desktop.ini
.DS_Store
